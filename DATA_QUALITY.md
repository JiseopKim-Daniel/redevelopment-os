# DATA QUALITY - Redevelopment OS

## 1. 목적

이 문서는 데이터의 수집, 보존, 정규화, 검증, 게시, 정정과 롤백에 적용할 품질 기준을 정의한다. 핵심 목표는 모든 게시 사실을 원 출처까지 추적하고, 공식 정보의 변경 이력을 잃지 않으며, 사람의 판단·추론·AI 해설이 공식 사실과 섞이거나 이를 덮어쓰지 않도록 하는 것이다.

## 2. 데이터 계층과 분리 원칙

데이터는 최소한 다음 네 종류로 명시적으로 분리한다.

| 종류 | 예시 | 저장·표시 규칙 | 공식 사실 변경 권한 |
|---|---|---|---|
| 공식 사실 (`official_fact`) | 고시된 사업 단계, 지정일, 인가일, 공식 경계 | 원문과 식별자를 필수 연결하고 검증 상태 표시 | 승인된 공식 변경 레코드만 가능 |
| 사용자 입력 (`user_entered`) | 개인 메모, 직접 확인한 민간 호가, 가정한 비용 | 입력자·입력시각·출처·확인일을 기록하고 공식 데이터와 별도 표시 | 없음 |
| 추론값 (`inferred`) | 주소 매칭 결과, 예상 일정, 계산 점수, 대표 좌표 | 산식·모델 버전·입력 데이터·신뢰도를 기록하고 `추정`으로 표시 | 없음 |
| AI 생성 해설 (`ai_generated`) | 요약, 리스크 코멘트, 비교 설명 | 모델·프롬프트 버전·생성시각·근거 레코드를 기록하고 `AI 생성`으로 표시 | 없음. 공식 사실을 절대 덮어쓸 수 없음 |

민간 매물 호가는 `official_fact`가 아니다. 라이선스 피드는 `licensed_private`, 사용자가 기록한 호가는 `user_entered`로 관리하며 실거래가와 별도의 필드·화면·집계로 취급한다.

## 3. 공식 출처 우선 원칙

1. 최신의 유효한 고시·공고·공부 원문을 최우선으로 한다.
2. 공식 API의 값과 원문 고시가 충돌하면 법적 효력, 정정 여부와 기준일을 확인할 때까지 자동 게시하지 않는다.
3. 보도자료, 조합 공지, 뉴스는 단계 변경의 단독 확정 근거로 사용하지 않는다.
4. 출처 우선순위가 같다면 더 구체적인 대상 범위, 더 최신의 `effective_date`, 정정·변경 문서를 우선한다.
5. 정상적인 공식 API 관측값은 출처·스키마·중복·참조 무결성·신뢰도·충돌 검사를 모두 통과하면 `auto_checked` 상태로 게시할 수 있다. 다만 중요 공식 사실의 변경은 자동화가 후보만 제안하며 사람이 승인해야 한다.

## 4. 원시 데이터 보존

- 수집한 원본 응답, 파일 또는 문서 메타데이터를 변경 불가능한 원시 레코드로 보존한다.
- 원본 저장이 이용조건이나 저작권상 허용되지 않으면 전문을 저장하지 않고 URL, 식별자, 해시, 응답 메타데이터와 허용된 발췌만 보존한다.
- 재수집 결과가 달라도 기존 원본을 덮어쓰지 않고 새 버전을 추가한다.
- 파싱 실패 원본도 장애 분석과 재처리를 위해 보존 정책에 따라 유지한다.

### 4.1 보존 기간

- 핵심 공식 고시·공고의 식별자, 출처 URL, 제목, 발행일, 효력일, 콘텐츠 해시와 변경 관계 메타데이터는 영구 보존한다.
- 원시 API 응답은 MVP 초기 기준 최소 1년 보존한다. 저장 비용과 재현 가능성을 검토해 이후 기간을 조정할 수 있다.
- 실패 작업 로그는 최소 90일 보존한다. 반복 장애나 감사 대상과 연결된 로그는 해결 또는 감사 종료 시까지 연장할 수 있다.
- 뉴스는 저장 허가가 확인되지 않는 한 제목 등 최소 메타데이터와 원문 URL만 보존하고 본문은 저장하지 않는다.
- 제공자 이용약관, 라이선스, 개인정보 또는 법적 삭제 의무가 이 기준과 충돌하면 제공자 조건과 법적 의무가 항상 우선한다.

## 5. 정규화 데이터 저장

원시 데이터와 서비스용 정규화 데이터를 분리한다. 권장 논리 계층은 다음과 같다.

1. `source_registry`: 제공자, 데이터셋, 이용조건, 자동화 허용 상태
2. `source_documents`: 고시·공고·API 문서 단위의 URL, 제공자 식별자, 제목, 발행일과 효력일
3. `raw_observations`: 수집 단위의 불변 원본·해시·수집 결과
4. `normalized_observations`: 파싱·주소 표준화·단위 변환을 거친 관측값
5. `review_records`: 충돌, 중요 변경, 수동 검토와 승인 기록
6. `published_facts`: 검사를 통과해 현재 서비스에 노출되는 공식 사실
7. `fact_history` / `stage_history`: 공식 사실과 단계의 전체 유효기간 이력
8. `user_values`, `inferred_values`, `ai_commentary`: 비공식 계층별 저장소
9. `audit_logs`: 생성·변경·승인·게시·롤백 행위 기록

출처 메타데이터 전체를 각 업무 테이블에 복제하지 않는다. `source_registry`가 제공자를, `source_documents`가 개별 원문 또는 API 문서를 관리하고, 관측값은 `source_document_id`와 `raw_observation_id`로 이를 참조한다. 각 정규화 레코드는 하나 이상의 원시 관측값과 출처 문서까지 추적 가능해야 하며, 출처 없는 공식 사실은 게시할 수 없다.

## 6. 필수 출처 및 시간 필드

`source_url`과 `source_identifier`는 `source_documents`에 한 번 저장한다. 관측값은 이를 복제하지 않고 `source_document_id`로 참조한다. 다음 정보는 관측값에서 추적 가능해야 하며, 반드시 모든 업무 테이블에 열로 중복할 필요는 없다.

| 필드 | 정의 | 저장 위치 | 규칙 |
|---|---|---|---|
| `source_document_id` | `source_documents`의 원문 또는 API 문서 참조 | 관측 레코드 | 관측값에 필수. 이를 통해 URL과 제공자 식별자를 조회 |
| `source_url` | 원문 또는 공식 데이터셋으로 연결되는 URL | `source_documents` | 가능하면 영구 링크 사용. URL이 없으면 사유 기록 |
| `source_identifier` | 고시번호, 공고번호, API 레코드 키, 문서번호 등 제공자 식별자 | `source_documents` | 제공되는 경우 필수이며 제공자 범위에서 유일성 검사 |
| `fetched_at` | 시스템이 원본을 취득한 시각 | `raw_observations` | UTC 타임스탬프, 자동 기록, 수정 금지 |
| `effective_date` | 사실이 법적·업무상 효력을 갖기 시작한 날짜 | `normalized_observations` 또는 `published_facts` | 게시일·수집일과 구분. 불명확하면 `null`과 사유 기록 |
| `last_verified_at` | 사람이 원문과 정규화 결과를 마지막으로 대조한 시각 | `review_records` 또는 `published_facts` | 실제 사람 검토 후에만 검토자와 함께 기록 |
| `verification_status` | 검증 진행 상태 | `normalized_observations` 또는 `published_facts` | 아래 상태 집합만 사용 |
| `collection_method` | API, 공식 파일, 수동 입력 등 취득 방식 | `raw_observations` | 자동 수집 관측값에 필수 |
| `parser_version` | 원본을 해석한 파서 또는 매핑 규칙 버전 | `raw_observations` 또는 `normalized_observations` | 자동 수집 관측값에 필수. 재현 가능한 버전 식별자 사용 |
| `confidence_level` | 매칭·파싱 결과의 신뢰 수준 | `normalized_observations` | `high`, `medium`, `low` 등 제한된 값과 산정 규칙 사용 |

`verification_status` 권장 값:

- `unverified`: 수집·입력되었으나 검증되지 않음
- `auto_checked`: 출처·스키마·중복·참조 무결성·신뢰도·충돌 검사를 통과함. 정상적인 공식 API 관측값은 이 상태로 게시 가능
- `review_required`: 중요 변경, 충돌 또는 낮은 신뢰도로 사람 검토 필요
- `verified`: 사람이 원문과 대조해 승인함
- `rejected`: 잘못된 매칭, 중복 또는 신뢰 불가로 기각됨
- `superseded`: 더 최신의 유효 레코드로 대체되었으나 이력 보존됨
- `stale`: 최신성 기준을 초과함

## 7. 오래된 데이터 규칙

- 데이터 범주마다 `expected_refresh_interval`과 `stale_after`를 설정한다.
- 예정 갱신 주기의 2배 동안 성공적인 확인이 없으면 기본적으로 `stale` 후보로 표시한다. 법적 기간이 있는 지정 정보는 종료일 도래 시 즉시 재검증한다.
- 수집 실패와 `변경 없음`은 구분한다. 수집 실패 시 `last_verified_at`을 갱신하지 않는다.
- 오래된 데이터는 삭제하지 않으며, 화면 사용 시 마지막 확인일과 오래됨 상태를 함께 제공한다.
- 사업 단계, 인허가, 토지거래허가구역, 공식 경계가 오래된 경우 신규 투자 판단용 `verified` 상태로 간주하지 않는다.

범주별 구체 임계치는 실제 API의 갱신 주기와 운영 수준협약을 확인한 뒤 설정한다.

## 8. 충돌 감지

다음 경우 충돌 레코드를 생성한다.

- 동일 구역·동일 속성·겹치는 유효기간에 서로 다른 공식 값이 존재함
- API 값이 최신 고시·공고 원문과 다름
- 변경·정정·취소 문서가 기존 게시 사실과 연결되지 않음
- 주소 또는 공간 매칭 결과가 둘 이상의 구역을 가리킴
- 공식 경계와 필지·주소 판정이 불일치함
- 하위 우선순위 출처가 상위 출처의 사실과 다름

충돌 시 새 값은 `review_required`로 격리하고 현재 게시값을 자동 덮어쓰지 않는다. 검토자는 양쪽 원문, 문서일, `effective_date`, 대상 범위와 정정 관계를 확인한 후 승인·기각·병합한다.

## 9. 중복 감지

- 1차 키: `provider + source_identifier + record_type`
- 보조 키: 구역 식별자, 문서번호, `effective_date`, 속성명, 정규화 값
- 원문 중복: 정규화된 콘텐츠 해시와 파일 해시
- 거래 중복: 제공자의 거래 키를 우선하고, 없으면 법정동·단지·계약일·면적·층·금액 등 복합키를 사용
- 뉴스·공지: 정규화 URL, 제목, 발행시각, 본문 해시를 조합

중복 후보는 자동 삭제하지 않는다. 동일 원문의 재수집은 한 관측 그룹으로 연결하고, 서로 다른 출처의 동일 사실은 교차 근거로 보존한다.

## 10. 주소 정규화

1. 입력 원문 주소를 `raw_address`로 그대로 보존한다.
2. 도로명주소와 지번주소를 공식 주소 데이터 기준으로 정규화한다.
3. 시도, 시군구, 법정동, 행정동, 본번, 부번, 건물관리번호 등 구성요소를 가능한 범위에서 분리한다.
4. 주소 변환 API의 결과, 점수, 후보 목록, 변환 시각과 제공자를 기록한다.
5. 다중 후보, 낮은 신뢰도, 행정동·법정동 혼동은 `review_required`로 보낸다.
6. 구역이 다수 필지를 포함할 때 대표 주소 하나로 구역 포함 여부를 확정하지 않는다.

대표 위도·경도는 탐색과 지도 중심점 용도의 `representative_point`로 저장한다. 공식 정비구역 경계는 별도의 버전 관리된 `official_boundary` 기하로 저장하고, 대표 좌표나 임의 반경을 공식 경계로 표시하지 않는다.

## 11. 사업 단계 변경 이력

- `stage_history`를 사업 단계의 단일 진실 공급원(single source of truth)으로 사용한다.
- 모든 단계 관측은 `stage_history`에 단계, 이전 단계, 근거 문서, `effective_date`, 수집·검증 시각과 상태를 추가한다.
- 단계 정정·취소도 삭제가 아니라 새 이력과 이전 레코드의 `superseded` 관계로 표현한다.
- 단계의 역행, 건너뜀, 동일 날짜의 복수 단계, 과거 효력일의 늦은 발견은 자동 게시하지 않고 검토한다.
- `areas.current_stage`는 게시 가능한 `stage_history`에서 계산한 파생 캐시일 뿐이며 사람이 직접 입력하거나 수정해서는 안 된다. 이력과 불일치하면 언제든 재구축 가능해야 한다.

### 11.1 구역 생애주기 상태

사업 단계와 구역 자체의 생애주기를 혼합하지 않는다. 별도의 `lifecycle_status`는 다음 값만 사용한다.

- `active`: 현재 유효하게 추적 중
- `suspended`: 공식적으로 중단 또는 보류됨
- `cancelled`: 지정·사업 등이 취소됨
- `released`: 지정 또는 규제 대상에서 해제됨
- `merged`: 다른 구역과 병합됨
- `superseded`: 새 구역 정의나 레코드로 대체됨

생애주기 변경도 근거 문서와 효력일을 보존한다. `cancelled`, `released`, `merged`, `superseded`로의 변경은 중요 공식 변경으로 취급해 게시 전 수동 검토를 거친다.

### 11.2 대규모 수치 변경 감지

다음 초기 운영 기본값을 초과하는 변경은 이상 변경 후보로 표시하고 `review_required`로 보낸다.

- 세대수: 이전 게시값 대비 20% 이상
- 구역 또는 사업 면적: 이전 게시값 대비 10% 이상
- 용적률: 20%p 이상 또는 이전 게시값 대비 10% 이상 중 먼저 충족하는 기준
- 사업비: 이전 게시값 대비 20% 이상

이 값들은 오류와 중요 변경을 조기에 찾기 위한 설정 가능한 운영 기본값이며 법적 기준이 아니다. 데이터 특성과 운영 경험에 따라 범주·사업 유형별로 조정하고, 사용된 임계치 버전을 검토 레코드에 남긴다.

## 12. 수동 검토 워크플로

다음 변경은 게시 전에 반드시 `review_record`를 생성한다.

- 정비구역 지정·해제 또는 공식 경계 변경
- 추진위원회 승인, 조합설립인가, 사업시행계획인가, 관리처분계획인가 등 핵심 사업 단계 변경
- 토지거래허가구역 지정·연장·해제 또는 적용 범위 변경
- 구역 식별자 병합·분리, 주소·공간 매칭의 중대한 변경
- 공식 세대수·면적 등 핵심 수치의 큰 변동
- 공식 출처 간 충돌 또는 정정·취소 문서 발견

그 밖의 정상적인 공식 API 관측값은 출처·스키마·중복·참조 무결성·충돌 검사를 통과하고 신뢰도 기준을 충족하면 `auto_checked` 상태로 게시할 수 있으며, 건별 사람 검토를 요구하지 않는다.

워크플로:

1. 자동화 또는 사용자가 후보 변경을 생성한다.
2. 시스템이 원본, 이전값, 새 값, 차이, 출처 우선순위와 영향 범위를 묶어 검토 레코드를 만든다.
3. 검토자가 공식 원문과 대상 범위를 확인한다.
4. 검토자는 승인, 기각, 추가 자료 요청 중 하나를 선택하고 근거 메모를 남긴다.
5. 승인된 변경만 게시 사실과 현재값 캐시에 반영한다.
6. 게시 후 감사 로그와 이전 버전을 유지한다.

MVP에서는 한 명의 지정 검토자가 승인한다. 2인 승인은 운영 규모와 위험 수준이 커질 때 도입할 수 있는 향후 옵션이며 MVP 요구사항이 아니다.

## 13. 감사 로그

다음 행위를 추가 전용 감사 로그에 기록한다.

- 수집 실행 시작·종료·실패
- 원본 생성과 파서·정규화 버전
- 자동 검증 결과와 충돌·중복 판정
- 수동 생성·수정·승인·기각
- 게시, 게시 취소, 롤백
- 사용자 입력과 AI 생성물의 생성·재생성·삭제

로그에는 행위자 또는 작업 ID, 시각, 대상 레코드, 이전값과 새 값의 해시 또는 변경분, 사유, 관련 검토 레코드를 포함한다. 운영자는 감사 로그를 일반 데이터 수정 기능으로 변경할 수 없어야 한다.

## 14. 재시도와 실패 처리

- 일시적 네트워크 오류와 제공자 제한에는 지수 백오프와 무작위 지연을 적용한다.
- 인증 오류, 이용 한도 초과, 스키마 변경, 파싱 실패는 무한 재시도하지 않고 정해진 횟수 후 격리 큐로 보낸다.
- 제공자의 `Retry-After`와 호출 한도를 준수한다.
- 작업은 멱등성을 가져야 하며 같은 입력 재처리가 중복 게시를 만들지 않아야 한다.
- 일부 레코드 실패 시 성공분과 실패분을 구분하되, 불완전한 배치가 현재 공식 사실을 삭제하거나 전체 데이터로 오인되지 않게 한다.
- 연속 실패, 예상 레코드 수 급감, 콘텐츠 해시의 비정상 변화는 경고를 만들고 자동 게시를 중단한다.
- 실패 시 기존 검증 데이터를 유지하고 `fetched_at` 또는 `last_verified_at`을 성공한 것처럼 갱신하지 않는다.

## 15. 롤백

- 원시 데이터, 정규화 관측값, 게시 사실 버전과 이력을 삭제하지 않는 방식으로 롤백한다.
- 게시 릴리스 또는 변경 묶음마다 식별자를 부여해 특정 승인 시점으로 되돌릴 수 있게 한다.
- 롤백은 이전 검증 버전을 새 현재 버전으로 재지정하며, 롤백 행위와 사유를 감사 로그에 남긴다.
- 잘못된 단계 변경 롤백 시 `stage_history`를 삭제하지 않고 취소·대체 관계를 추가한다.
- 롤백 전후에 파생 점수, 캐시, 검색 인덱스, AI 해설을 무효화하거나 재계산 대상으로 표시한다.
- 공식 사실이 롤백되더라도 사용자 입력은 별도 계층에 유지하되, 더 이상 유효하지 않은 공식 사실을 참조하면 경고한다.

## 16. AI 안전 규칙

1. AI 생성 콘텐츠는 읽기 전용 공식 사실을 입력으로 사용하고 별도 저장한다.
2. AI는 `published_facts`, `stage_history`, 공식 경계 또는 출처 메타데이터를 생성·수정·승인할 권한이 없다.
3. AI 출력은 근거 레코드와 생성 시점에 고정하며, 근거가 변경되면 오래됨으로 표시한다.
4. 근거가 없거나 충돌 중인 내용은 사실처럼 단정하지 않는다.
5. AI 요약의 수정·재생성은 이전 버전을 보존하며 공식 사실을 절대 덮어쓰지 않는다.

## 17. 게시 전 최소 품질 기준

공식 사실은 다음 조건을 모두 만족할 때만 게시할 수 있다.

- 등록된 공식 출처와 연결됨
- `source_document_id`를 통해 `source_url` 또는 URL 부재 사유와 `source_identifier`를 조회할 수 있고, `fetched_at`이 있음
- 정규화와 중복 검사가 완료됨
- 충돌이 해결됨
- 중요한 변경인 경우 검토 레코드가 승인됨
- 정상적인 공식 API 관측값은 `verification_status = auto_checked` 이상이며, 중요 변경은 `verification_status = verified`
- 효력일을 알 수 있으면 `effective_date`가 기록됨
- 마지막 성공 수집·자동 검사 시점이 해당 범주의 오래됨 기준을 넘지 않으며, 사람 검증 대상이면 `last_verified_at`도 기준을 넘지 않음

자동 수집 관측값은 추가로 `collection_method`, `parser_version`, `confidence_level`이 있어야 하며, 게시에 필요한 최소 신뢰도 기준을 충족해야 한다. `last_verified_at`은 사람이 실제 대조한 경우에만 기록하며, 정상적인 `auto_checked` 게시를 위해 형식적으로 생성하지 않는다.
